using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Widget script, not really important since Movement handles most of her functions along with her layermask, but if you want to add extra its here
public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update

    private void Awake()
    {
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }
}
